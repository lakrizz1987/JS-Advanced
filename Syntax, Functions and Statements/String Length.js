function strLength(word1,word2,word3) {
    let sumLengths = word1.length + word2.length + word3.length;
    let averageSum = Math.floor(sumLengths/3);

    console.log(sumLengths);
    console.log(averageSum);
}